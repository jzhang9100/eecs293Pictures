The main algorithim to find the pictures is in main.java

To run it you need to create pictures, add the pictures to a PictureCollection, and pass the PictureCollection which will  return a PictureCollection, with the optimal pictures
